# Sécurité pnpm
# Bloquer les lifecycle scripts (postinstall, preinstall, etc.)
ignore-scripts=true

# Safe-chain : bloquer les packages de moins de 7 jours
# (activé par défaut dans pnpm 10+, on le rend explicite)
safe-chain=true
safe-chain-skip-minimum-package-age=false
